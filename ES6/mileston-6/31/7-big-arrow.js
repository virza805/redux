const tenTimes = (num) => num * 10;
const output = tenTimes(56);
console.log(output);

const fiveTimes = num => num * 5;
const value = fiveTimes(17);

const getName = () => 'Tanvir Hasan';
const naem = getName();
console.log(naem);

const doMath = (x, y) => {
    const sum = x + y;
    const diff = x - y;
    const result = sum * diff;
    const output = result * 5;
    return output;
}
const total = doMath(12, 5);
console.log(total);























